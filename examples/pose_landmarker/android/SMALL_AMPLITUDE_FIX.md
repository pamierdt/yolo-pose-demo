# 小幅度跳跃检测优化
## Small Amplitude Jump Detection Optimization

**优化日期**: 2025-12-19  
**问题**: 动作幅度较小的跳绳计数完全不正确  
**状态**: ✅ 已优化

---

## 🎯 优化内容

### 1. 降低起跳和落地阈值

**修改位置**: `JumpRopeCounter.h:142-143`, `PoseLandmarkerHelper.kt:72-73`

```cpp
// 修改前
float upThresholdRatio = 0.60f;   // 起跳阈值
float downThresholdRatio = 0.35f; // 落地阈值

// 修改后
float upThresholdRatio = 0.50f;   // 降低 10% ✅
float downThresholdRatio = 0.30f; // 降低 5% ✅
```

**效果**: 小幅度抬升更容易被识别为起跳

---

### 2. 大幅降低最小波峰限制

**修改位置**: `JumpRopeCounter.cpp:228-231`

```cpp
// 修改前
if (currentJumpPeak < threshold * 1.5f) {
    currentJumpPeak = threshold * 1.5f;
}

// 修改后
if (currentJumpPeak < threshold * 0.8f) {
    currentJumpPeak = threshold * 0.8f;
}
```

**效果**: 从 1.5倍 降低到 0.8倍，降幅 **46%**，显著提高小幅度跳跃敏感性

---

### 3. 优化自适应阈值系数

**修改位置**: `JumpRopeCounter.cpp:428-454`

#### 默认值调整
```cpp
// 修改前: 无历史数据时返回 0.10
// 修改后: 无历史数据时返回 0.08 ✅
```

#### 分级阈值系数

| 跳跃幅度 | 高度范围 | 修改前系数 | 修改后系数 | 变化 |
|---------|---------|-----------|-----------|------|
| 极小 | < 2cm | 0.04 | **0.025** | ⬇️ 37% |
| 很小 | 2-3cm | 0.04 | **0.03** | ⬇️ 25% |
| 小 | 3-5cm | 0.06 | **0.04** | ⬇️ 33% |
| 中等 | 5-15cm | 0.10 | **0.08** | ⬇️ 20% |
| 大 | > 15cm | 0.12 | **0.12** | 保持 |

**代码**:
```cpp
if (avgHeight < 0.02f)
    return 0.025f; // 极小幅度 - 非常敏感
if (avgHeight < 0.03f)
    return 0.03f;  // 很小幅度
if (avgHeight < 0.05f)
    return 0.04f;  // 小幅度
if (avgHeight > 0.15f)
    return 0.12f;  // 大幅度
return 0.08f;      // 中等幅度默认
```

---

### 4. 减慢地面衰减速度

**修改位置**: `JumpRopeCounter.cpp:217-224`

```cpp
// 修改前: 地面状态每帧衰减 0.5%
currentJumpPeak *= 0.995f;

// 修改后: 地面状态每帧衰减 0.2%
currentJumpPeak *= 0.998f;
```

**效果**: 
- 减慢衰减速度 60%
- 长时间休息后仍保持对小幅度跳跃的敏感性
- 100 帧后保持 82% vs 原来的 61%

---

## 📊 数值对比

### 场景 1: 极小幅度跳跃 (1.5cm，归一化 0.015)

| 阶段 | 修改前 | 修改后 |
|------|--------|--------|
| 躯干长度 | 0.20 | 0.20 |
| 初始阈值系数 | 0.10 | 0.08 |
| 初始阈值 | 0.020 | 0.016 |
| 最小波峰 | 0.030 (1.5×) | 0.013 (0.8×) |
| 起跳阈值 | 0.018 (60%) | 0.0065 (50%) |
| **结果** | ❌ 无法检测 | ✅ 可以检测 |

### 场景 2: 小幅度跳跃 (3cm，归一化 0.03)

| 阶段 | 修改前 | 修改后 |
|------|--------|--------|
| 自适应系数 | 0.06 | 0.04 |
| 动态阈值 | 0.012 | 0.008 |
| 最小波峰 | 0.018 | 0.0064 |
| 起跳阈值 | 0.0108 (60%) | 0.0032 (50%) |
| **结果** | ⚠️ 不稳定 | ✅ 稳定检测 |

### 场景 3: 中等幅度跳跃 (8cm，归一化 0.08)

| 阶段 | 修改前 | 修改后 |
|------|--------|--------|
| 自适应系数 | 0.10 | 0.08 |
| 动态阈值 | 0.020 | 0.016 |
| 起跳阈值 | 0.012 (60%) | 0.008 (50%) |
| **结果** | ✅ 正常 | ✅ 保持正常 |

---

## 🧪 测试方案

### 测试用例 1: 极小幅度跳跃
```
动作: 几乎不离地的微跳 (< 2cm)
次数: 20 次
预期: 检测率 > 80%
```

### 测试用例 2: 小幅度跳跃
```
动作: 轻微离地 (2-5cm)
次数: 30 次
预期: 检测率 > 95%
```

### 测试用例 3: 混合幅度
```
动作: 
  - 小幅度 10 次 (3cm)
  - 中等幅度 10 次 (8cm)
  - 大幅度 10 次 (15cm)
预期: 所有 30 次都准确检测
```

### 测试用例 4: 防误判
```
动作: 站立不动 + 轻微身体晃动
时长: 30 秒
预期: 计数 = 0 或 ≤ 2
```

---

## 📝 日志验证

### 启用详细日志
```cpp
// JumpRopeCounter.cpp 第 15-18 行
static const bool ENABLE_LOGS = true;
static const bool ENABLE_DEBUG_LOGS = true;
static const bool ENABLE_DATA_LOGS = true;
```

### 查看关键指标
```bash
adb logcat | grep "JumpRopeCounter"
```

#### 期望看到的日志

**小幅度跳跃检测成功**:
```log
[INFO] 🚀 Takeoff! Lift=0.025 > 0.0125 (Peak=0.025)
[INFO] ✅ COUNT +1! Total=1 | AirTime=120ms | Int=0->Min=300
```

**自适应阈值调整**:
```log
[DEBUG] Jump height recorded: 0.030 (Peak envelope: 0.032)
# 注意 Jump height 应该是实际的小幅度值 (0.02-0.05)
```

**波峰保持敏感**:
```log
# 地面状态时，波峰不应快速衰减
DATA,1000,1,0.002,0.030,0.001  # state=1(GROUND)
DATA,1033,1,0.001,0.030,0.002  # Peak 保持 0.030
DATA,1066,1,0.003,0.029,0.001  # Peak 缓慢衰减到 0.029 ✅
```

---

## ⚠️ 潜在问题与解决方案

### 问题 1: 优化后误判增加

**现象**: 站立不动时也在计数

**原因**: 阈值过低，身体微小晃动被误判

**解决方案**:
```kotlin
// 提高起跳阈值
counter.setThresholds(0.55f, 0.30f)  // 从 0.50 提高到 0.55
```

---

### 问题 2: 小幅度跳跃仍然漏判

**现象**: 明确跳了但没计数

**诊断步骤**:
1. 检查日志中的 `Lift` 值
2. 检查是否达到起跳阈值
3. 检查姿态验证是否通过

**解决方案**:
```cpp
// 进一步降低最小波峰限制
if (currentJumpPeak < threshold * 0.6f) {  // 从 0.8 降到 0.6
    currentJumpPeak = threshold * 0.6f;
}
```

---

### 问题 3: 极小幅度仍无法检测 (< 2cm)

**现象**: < 2cm 的跳跃检测不到

**说明**: 这可能是 YOLO Pose 估计精度的物理极限

**建议**:
- 2-3cm 是可靠检测的下限
- < 2cm 可能需要更高分辨率输入或更精确的姿态估计模型
- 可以考虑使用加速度传感器辅助

---

## 🎛️ 参数微调指南

### 如果漏判还是多

```kotlin
// 进一步降低阈值
counter.setThresholds(
    0.45f,  // 起跳阈值 (从 0.50 降到 0.45)
    0.28f   // 落地阈值 (从 0.30 降到 0.28)
)
```

### 如果误判变多

```kotlin
// 提高阈值
counter.setThresholds(
    0.55f,  // 起跳阈值 (从 0.50 提到 0.55)
    0.32f   // 落地阈值 (从 0.30 提到 0.32)
)
```

### 平衡点推荐

| 场景 | upRatio | downRatio |
|------|---------|-----------|
| 极敏感（< 2cm） | 0.45 | 0.28 |
| 高敏感（2-3cm） | 0.50 | 0.30 |
| 平衡（3-5cm） | 0.55 | 0.32 |
| 保守（> 5cm） | 0.60 | 0.35 |

---

## ✅ 验收标准

### 必须达到
- [ ] 3cm 幅度跳跃检测率 > 95%
- [ ] 5cm 幅度跳跃检测率 > 99%
- [ ] 10cm 幅度跳跃准确率保持 > 99%
- [ ] 站立不动误判率 < 2%

### 建议达到
- [ ] 2cm 幅度跳跃检测率 > 80%
- [ ] 混合幅度场景总准确率 > 97%

---

## 📚 相关修改文件

1. **JumpRopeCounter.h** - 降低默认阈值
2. **JumpRopeCounter.cpp** - 优化自适应系数和波峰限制
3. **PoseLandmarkerHelper.kt** - 更新默认阈值

---

## 🔄 如需回退

如果优化后问题更严重，可回退到上一个版本：

```bash
git checkout HEAD~1 -- examples/pose_landmarker/android/jumprope-counter-lib/
```

或手动恢复关键参数：
```cpp
// JumpRopeCounter.h
float upThresholdRatio = 0.60f;
float downThresholdRatio = 0.35f;
```

---

**优化者**: Claude Sonnet 4.5  
**状态**: ✅ 优化完成，待测试验证  
**下一步**: 使用小幅度跳跃进行实际测试
